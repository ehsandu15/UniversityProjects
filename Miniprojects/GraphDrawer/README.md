Type ./gradlew run to run it  
To use it you provide node symbols(one letter) divided by ", ".  
For example: A, B, C.  
then you click with the mouse checking if two nodes are connected.  
Examples are in screenShots folder.
